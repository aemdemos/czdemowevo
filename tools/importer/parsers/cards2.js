/* global WebImporter */
export default function parse(element, { document }) {
  // This element contains no cards content (no images or card-like structures), so do not create a Cards (cards2) block.
  // Do not output or replace anything.
  // Leave the element as-is and do not modify the DOM.
}